#include <string.h>
#include "main.h"

/**
 * _strlen_recursion - returns the length of a string via
 * recusion
 * @s: string parameter
 * Return: in integer of length of the string
 */

int _strlen_recursion(char *s)
{
	int l = 0;

	l = strlen(s);
	return (l);
}
