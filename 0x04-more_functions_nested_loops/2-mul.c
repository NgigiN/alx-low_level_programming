#include "main.h"
#include <stdio.h>

/**
 * mul - gives the product of two numbers
 * @a: number determined by user
 * @b: number determined by user
 * Return: 0 Success
 */

int mul(int a, int b)
{
	int c;

	c = a * b;
	printf("%d", c);
	return (0);
}
